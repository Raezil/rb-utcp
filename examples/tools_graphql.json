{
  "version": "1.0",
  "tools": [
    {
      "name": "country_by_code",
      "description": "Query country name by ISO code using a public GraphQL API",
      "inputs": { "type": "object", "properties": { "code": { "type": "string" } }, "required": ["code"] },
      "outputs": { "type": "object" },
      "tags": ["graphql", "demo"],
      "tool_provider": {
        "provider_type": "graphql",
        "url": "https://countries.trevorblades.com/",
        "query": "query ($code: ID!) { country(code: $code) { name, capital, currency } }",
        "operationName": "Country"
      }
    }
  ]
}
